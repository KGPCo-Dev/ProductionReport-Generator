
services:

  web:

    build: .
 
    working_dir: /app/production_report

    command: python manage.py runserver 0.0.0.0:8000

    volumes:
      - .:/app

    ports:
      - "9000:8000"

    env_file:
      - ./production_report/.env
